/*
*OnlineJudge
*10172 - The Lonesome Cargo Distributor
*Code by: Amanda Lim
*/

#include <bits/stdc++.h>
using namespace std;

int main ()
{
    int SET;
    cin >> SET;
    for (int set = 0; set < SET;) {
        int N, S, Q;
        cin >> N >> S >> Q;
        while (set < SET) {
            int Q;
            cin >> Q;
            for (int q = 0; q < Q; q++) {
                
            }
            set++;
        }
    }
    return 0;
}