/*
*OnlineJudge
*1124 - Celebrity jeopardy
*Code by: Amanda Lim
*/

#include <bits/stdc++.h>
using namespace std;

int main ()
{
    char c;
    while (cin.peek() != EOF) { // or while (1), if (c == EOF) break
        c = getchar();
        printf("%c", c);
    }
    // I only realized that this problem involves a loop and not just I/O, sorry guys xD
    return 0;
}